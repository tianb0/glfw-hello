snippets

```bash
# add submodule
git submodule add https://github.com/glfw/glfw.git external/glfw
# then commit changes

# retrieve submodules for a newly cloned repo
git submodule update --init --recursive
# we can also put it into CMakeLists

```
